import { Card, Typography } from '@equinor/eds-core-react'
import { tokens } from '@equinor/eds-tokens'

export function MissionCard() {
    return (
        <Card variant="default" style={{ boxShadow: tokens.elevation.sticky }}>
            hello
        </Card>
    )
}
