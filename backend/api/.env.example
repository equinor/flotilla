ASPNETCORE_ENVIRONMENT=Local
Local__DevUserId=
SARA__BaseUrl=http://localhost:8100/
SARA__Scopes__0=
# Both written by ../../setup.sh from the generated broker credentials. The key
# vault is read after the environment variables, so leaving it on would override
# the local password with the shared one.
# Mqtt__Password=
# KeyVault__UseKeyVault=false
