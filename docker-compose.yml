version: '3.7'
services:
  frontend:
    build: frontend
    ports:
      - '3001:3001'
  broker:
    build: broker
    ports:
    - '1883:1883'
    - '9001:9001'
  backend:
    build: backend
    ports:
      - '8000:8000'
